import type { LaundryMapMarkerProps } from './types';

// LaundryMapMarker is a native map component and does not render on web.
// This stub satisfies type-safe imports in shared code paths.
export const LaundryMapMarker = (_props: LaundryMapMarkerProps): null => null;
